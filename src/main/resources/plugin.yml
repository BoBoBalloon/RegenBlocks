name: RegenBlocks
version: '${project.version}'
main: me.boboballoon.regenblocks.RegenBlocks
api-version: 1.19
authors: [BoBoBalloon]
description: a plugin that regenerates blocks

commands:
  regenblock:
    description: A command used to set a regen block
    aliases: [rb]
    permission: regenblocks.command